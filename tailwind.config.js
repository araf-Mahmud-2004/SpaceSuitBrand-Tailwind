module.exports = {
    mode: 'jit',  // Enable JIT mode
    purge: ['./**/*.html'],  // Adjust the purge paths if necessary
    theme: {
      extend: {},
    },
    variants: {
      extend: {},
    },
    plugins: [],
  }